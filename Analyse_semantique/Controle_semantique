- typage -> affectation
         -> calculs 
	 -> passage de params
	 -> retour fct incorrect-> liste des types qui pvt retournées (if imbriqués)

- Il existe ID -> variable
	       -> fonction
	       -> champ struct
	       -> type defini

- nb param appel fonction
- multiples declarations -> var, type, struct
			 -> fonctions
			 -> types

- depassement de tab statique pas crte
- booleen ds condition
- for (int): begin & end
- taille de tabeau < 0
- taille de tableau non int
- pas de retour dans fonction typé




En retour : const/var;
