let 
	type intArray = array of int
	var tab := intArray[XX] of 0
	var tab := intArray[10] of YY

	type str = {rr:int}
	var str_inst := str{rr:=intArray} 

	var x:int :=ZZ
	var y:=54
	var b:=-42
	var Z:string:=toto
	function test(a:int)=(
		if x<0 & y=54 | Z=toto & Z=test | (x>=2 | x<>34 & y=45 & (Z<>Test | a<=x))
			then (x:=1; y:=x+y);
		x:=1-y;
		a:=x+24*y/a;
		x:= (x+1-1)*200/y*a)
		
in test(WW) end
